DATABASE_URL=postgresql+asyncpg://app:app@db:5432/benjamin
JWT_SECRET=change-me
JWT_EXPIRES_HOURS=24
ALLOW_REGISTRATION=false
# Chave partilhada para GET /admin/stats (ver backend/app/security.py::require_admin).
# Vazia = endpoint sempre a devolver 401. Gera algo como: openssl rand -hex 32
ADMIN_API_KEY=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SUMMARY_EMAIL_TO=
SCHEDULER_ENABLED=true
FINNHUB_API_KEY=
TWELVEDATA_API_KEY=
# Opcional - só necessária para o resumo do analista (Overview). Sem chave, o
# botão "Atualizar análise" devolve um erro claro em vez de rebentar a app.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
